#include <iostream>
#include <string>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;
    if (x * y >= 100) {
        cout << "Yes" << endl;
    } else {
        cout << "No" << endl;
    }
    return 0;
}
