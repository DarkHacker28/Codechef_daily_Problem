#include<bits/stdc++.h>

using namespace std;

int main(){
    int a, b, x, y;
    cin >> a >> b >> x >> y;

    if(x < y)
        cout << b << endl;
    else
        cout << a << endl; 

    return 0;
}
