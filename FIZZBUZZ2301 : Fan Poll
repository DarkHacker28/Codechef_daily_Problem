#include<bits/stdc++.h>

using namespace std;

int main(){
    int a, b, c;
    cin >> a >> b >> c;

    if(max(max(a, b), max(b, c)) == a)
        cout << "YES" << endl;
    else
        cout << "NO" << endl;

    return 0;
}
