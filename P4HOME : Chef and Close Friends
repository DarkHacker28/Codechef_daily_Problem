#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int x, y, z;
        cin >> x >> y >> z;
        int answer = 2 * min(y, z);
        cout << answer << endl;
    }
    return 0;
}
