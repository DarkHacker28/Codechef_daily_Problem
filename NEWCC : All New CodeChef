#include<bits/stdc++.h>

using namespace std;

int main(){
    int x, y;
    cin >> x >> y;

    if(x > y)
        cout << "NEW" << endl;
    else if(x < y)
        cout << "OLD" << endl;
    else
        cout << "SAME" << endl;

    return 0;
}
