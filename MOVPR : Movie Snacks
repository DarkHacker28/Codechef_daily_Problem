#include<bits/stdc++.h>

using namespace std;

int main(){
    int x, y, z;
    cin >> x >> y >> z;

    if(z < (x + y))
        cout << ((2 * z) + y);
    else
        cout << ((2 * x) + (3 * y));

    return 0;
}
