#include <bits/stdc++.h>

using namespace std;

int main() {
    int t = 1;
    // cin >> t;

    while(t--){
        int a, b;
        cin >> a >> b;
        
        if(a > b)
            cout << "Dom" << endl;
        else            
            cout << "Tyro" << endl;
    }

    return 0;
}
  
