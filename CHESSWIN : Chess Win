#include <bits/stdc++.h>
using namespace std;

int main() {
	int x,y;
	cin>>x>>y;
	if(x==y)
	 cout<<x+1<<endl;
	else if(x>y)
	 cout<<"0"<<endl;
	else
	{
	 int z=y-x;
	 cout<<z+1<<endl;
	}
	return 0;
}
